
export enum data {
    TEXT = 'text',
    CHECKBOX = 'checkbox',
    SELECT = 'select',
    DATE = 'date',
    LABEL = 'inputLabel'
}

export const labels={
    LABEL: /^[_a-zA-Z][_a-zA-Z0-9]{0,30}$/i,
    KEY: /^[a-zA-Z][a-zA-Z0-9]{0,8}$/i
}